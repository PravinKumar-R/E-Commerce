import { Address } from "./address";

export class Seller {
    sellerId!: number;
    sellerName!: String;
    sellerRate!: number;
    addedDate!: Date;
    sellerPhoneNumber!: number;
    addressId!: Address;
}